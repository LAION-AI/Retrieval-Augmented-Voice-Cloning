# Voice Acting Pipeline Dependencies
# Most are already installed in the base environment

# FastAPI servers
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.6

# HTTP client
requests>=2.28.0
aiohttp>=3.8.0

# Audio I/O
soundfile>=0.12.0
librosa>=0.10.0
scipy>=1.10.0

# ML/AI
torch>=2.0.0
numpy>=1.24.0
transformers>=4.35.0
huggingface-hub>=0.20.0
safetensors>=0.4.0

# Echo TTS (open-source TTS model)
# pip install git+https://github.com/julien-blanchon/open-echo-tts.git

# DACVAE (for dataset audio encoding/decoding)
# pip install git+https://github.com/kadirnar/fast-dacvae.git

# VLLM (for LLM-based sentence generation)
# pip install vllm openai

# ChatterboxVC (zero-shot voice conversion, requires separate Python 3.13 venv)
# See: https://github.com/LAION-AI/chatterbox-voice-conversion
